#ifndef TYPES_H
#define TYPES_H

#include <Arduino.h>

// AudioTask and its priority queue left with the download engine: playback
// arrives only as pushed PCM now, so there is nothing to schedule.

#endif
